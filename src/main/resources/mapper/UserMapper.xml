<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="get.to.the.point.pilot01.mapper.UserMapper">

    <!-- ID로 사용자 조회 -->
    <select id="getUserById" parameterType="long" resultType="get.to.the.point.pilot01.entity.User">
        SELECT * FROM user WHERE id = #{id} AND is_active = true
    </select>

    <!-- 이메일로 사용자 조회 -->
    <select id="getUserByEmail" parameterType="string" resultType="get.to.the.point.pilot01.entity.User">
        SELECT * FROM user WHERE email = #{email} AND is_active = true
    </select>

    <!-- 사용자 추가 -->
    <insert id="insertUser" parameterType="get.to.the.point.pilot01.entity.User" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO user (name, email, password, created_at, updated_at, is_active)
        VALUES (#{name}, #{email}, #{password}, #{createdAt}, #{updatedAt}, #{isActive})
    </insert>

    <!-- 사용자 정보 업데이트 -->
    <update id="updateUser" parameterType="get.to.the.point.pilot01.entity.User">
        UPDATE user
        SET
            name = #{name},
            email = #{email},
            password = #{password},
            updated_at = #{updatedAt}
        WHERE
            id = #{id}
          AND is_active = true
    </update>

    <!-- 사용자 비활성화 -->
    <update id="deleteUser" parameterType="long">
        UPDATE user
        SET
            is_active = false,
            updated_at = CURRENT_TIMESTAMP
        WHERE
            id = #{id}
    </update>

</mapper>
